# ERS / SRS SIGBUNI

- Fuente principal: `SIGBUNI_ISR401.tex`
- Bibliografía: `referencias.bib`
- Figuras: `images/` (mockups, UML, i*, curva de saturación)

Compilación sugerida:

```bash
cd ERS
latexmk -pdf SIGBUNI_ISR401.tex
```
